[project]
name = "fastapi-track-kafka"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.135.3",
    "confluent-kafka>=2.14.0",
    "dotenv>=0.9.9",
    "jinja2>=3.1.6",
]
